<form method="post" action="{{ path('app_raiting_delete', {'id': raiting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ raiting.id) }}">
    <button class="btn">Delete</button>
</form>
