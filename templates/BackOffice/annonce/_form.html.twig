{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-info btn-default btn-squared btn-transparent-info ">{{ button_label|default('Save') }}
                                    </button>
    
{{ form_end(form) }}
