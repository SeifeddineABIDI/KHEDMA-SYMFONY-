{% extends 'base.html.twig' %}


{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('/addstyle.css') }}" />
{% endblock %}

{% block body %}
    <h1>Create New Task</h1>

    {{ form_start(form) }}

       <button type="submit">Create Task</button>
    {{ form_end(form) }}

    
    
    <a href="{{ path('app_tache_index', {'projetId': projetId}) }}">Cancel</a>
{% endblock %}



